BASE_URL=https://www.saucedemo.com         # Override with the preview branch
CI=true                                      # Enable CI mode (retries, single worker)
INCLUDE_MOBILE=false                          # Include mobile browsers in test runs (default: false)
CONFIG_NAME=login                   # Specify config file for test:config commands
SUITE_PATH=team-name/suite-name              # Specify test suite path for test:suite commands
DEVICE_ID=your-device-id                     # Device ID for API authentication (optional)